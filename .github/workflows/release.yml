name: Release

on:
  push:
    tags:
      - '[0-9]*.[0-9]*'
      - '[0-9]*.[0-9]*.[0-9]*'

jobs:
  release:
    runs-on: macos-26
    permissions:
      contents: write
    env:
      VERSION: ${{ github.ref_name }}
    steps:
      - uses: actions/checkout@v4

      - name: Import code signing certificate
        env:
          CERTIFICATE_P12: ${{ secrets.CERTIFICATE_P12 }}
          CERTIFICATE_PASSWORD: ${{ secrets.CERTIFICATE_PASSWORD }}
        run: |
          # Create a temporary keychain for CI
          security create-keychain -p "" /tmp/build.keychain
          security default-keychain -s /tmp/build.keychain
          security unlock-keychain -p "" /tmp/build.keychain
          security set-keychain-settings /tmp/build.keychain

          # Import the persistent certificate from secrets
          echo "$CERTIFICATE_P12" | base64 --decode > /tmp/cert.p12
          security import /tmp/cert.p12 -k /tmp/build.keychain -P "$CERTIFICATE_PASSWORD" -T /usr/bin/codesign

          # Extract the public cert for trust and trust it
          openssl pkcs12 -in /tmp/cert.p12 -passin pass:"$CERTIFICATE_PASSWORD" -clcerts -nokeys -legacy -out /tmp/cert.pem
          sudo security add-trusted-cert -d -r trustRoot -k /tmp/build.keychain /tmp/cert.pem
          security set-key-partition-list -S apple-tool:,apple: -s -k "" /tmp/build.keychain

          # Get the SHA-1 hash of the signing identity
          IDENTITY=$(security find-identity -v -p codesigning /tmp/build.keychain | head -1 | awk '{print $2}')
          echo "CODESIGN_IDENTITY=$IDENTITY" >> "$GITHUB_ENV"
          echo "Found identity: $IDENTITY"

          # Clean up
          rm -f /tmp/cert.p12 /tmp/cert.pem

      - name: Resolve packages
        run: xcodebuild -resolvePackageDependencies -project OpenWhisper/OpenWhisper.xcodeproj -scheme OpenWhisper

      - name: Archive
        run: |
          xcodebuild archive \
            -project OpenWhisper/OpenWhisper.xcodeproj \
            -scheme OpenWhisper \
            -configuration Release \
            -archivePath /tmp/OpenWhisper.xcarchive \
            CODE_SIGN_IDENTITY="${CODESIGN_IDENTITY}" \
            CODE_SIGNING_REQUIRED=YES \
            CODE_SIGN_STYLE=Manual \
            OTHER_CODE_SIGN_FLAGS="--keychain /tmp/build.keychain"

      - name: Sign app bundle with entitlements
        run: |
          APP_PATH="/tmp/OpenWhisper.xcarchive/Products/Applications/OpenWhisper.app"
          # Sign inside-out: XPC services, helper apps, frameworks, dylibs, then the main app.
          # Every nested component must be signed with the same identity before the parent.
          find "$APP_PATH/Contents/Frameworks" -name "*.xpc" -type d | while read xpc; do
            codesign --force --sign "${CODESIGN_IDENTITY}" --keychain /tmp/build.keychain \
              --options runtime "$xpc"
          done
          find "$APP_PATH/Contents/Frameworks" -name "*.app" -type d | while read app; do
            codesign --force --sign "${CODESIGN_IDENTITY}" --keychain /tmp/build.keychain \
              --options runtime "$app"
          done
          find "$APP_PATH/Contents/Frameworks" -name "*.framework" -type d -maxdepth 1 | while read fw; do
            codesign --force --sign "${CODESIGN_IDENTITY}" --keychain /tmp/build.keychain \
              --options runtime "$fw"
          done
          find "$APP_PATH/Contents/Frameworks" -name "*.dylib" -type f | while read lib; do
            codesign --force --sign "${CODESIGN_IDENTITY}" --keychain /tmp/build.keychain \
              --options runtime "$lib"
          done
          # Sign the main app last with entitlements
          codesign --force --sign "${CODESIGN_IDENTITY}" --keychain /tmp/build.keychain \
            --entitlements OpenWhisper/OpenWhisper/OpenWhisper.entitlements \
            --options runtime "$APP_PATH"
          # Verify deep signature
          codesign --verify --deep --strict --verbose "$APP_PATH"

      - name: Package .zip
        run: |
          cp -R "/tmp/OpenWhisper.xcarchive/Products/Applications/OpenWhisper.app" /tmp/OpenWhisper.app
          cd /tmp && ditto -c -k --keepParent OpenWhisper.app OpenWhisper.zip

      - name: Install Sparkle tools
        run: |
          curl -fsSL "https://github.com/sparkle-project/Sparkle/releases/download/2.9.0/Sparkle-for-Swift-Package-Manager.zip" \
            -o /tmp/sparkle-spm.zip
          unzip -q /tmp/sparkle-spm.zip -d /tmp/sparkle-spm

      - name: Sign update and generate appcast.xml
        env:
          SPARKLE_PRIVATE_KEY: ${{ secrets.SPARKLE_PRIVATE_KEY }}
        run: |
          DOWNLOAD_URL="https://github.com/${{ github.repository }}/releases/download/${VERSION}/OpenWhisper.zip"
          SIGNATURE=$(echo "$SPARKLE_PRIVATE_KEY" | /tmp/sparkle-spm/bin/sign_update --ed-key-file - -p /tmp/OpenWhisper.zip)
          FILE_SIZE=$(stat -f%z /tmp/OpenWhisper.zip)

          cat > /tmp/appcast.xml <<EOF
          <?xml version="1.0" encoding="utf-8"?>
          <rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle">
            <channel>
              <title>OpenWhisper</title>
              <link>https://github.com/${{ github.repository }}</link>
              <item>
                <title>Version ${VERSION}</title>
                <pubDate>$(date -R)</pubDate>
                <enclosure
                  url="${DOWNLOAD_URL}"
                  sparkle:version="${VERSION}"
                  sparkle:shortVersionString="${VERSION}"
                  sparkle:edSignature="${SIGNATURE}"
                  length="${FILE_SIZE}"
                  type="application/octet-stream" />
              </item>
            </channel>
          </rss>
          EOF

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$VERSION" \
            /tmp/OpenWhisper.zip \
            /tmp/appcast.xml \
            --title "OpenWhisper $VERSION" \
            --generate-notes
